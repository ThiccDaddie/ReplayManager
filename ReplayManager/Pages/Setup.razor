@page "/setup"

@if (OptionsMonitor.CurrentValue.ReplayDirectories.Any())
{
	<Redirect Url="/" />
}
else
{
	<DirectoryPicker ChosenPaths="@ChosenPaths"
					 FoundPaths="@FoundPaths"
					 OnFinish="@FinishSetup" />
}